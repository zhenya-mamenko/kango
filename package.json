{
  "name": "kango-chrome-extension",
  "version": "1.0.0",
  "description": "kanGO — Chrome extension for convenient fast hops for your long pages",
  "type": "module",
  "scripts": {
    "build": "rm -rf dist/ && vite build --mode production && node build.js --minify",
    "dev": "rm -rf dist/ && vite build --mode development && node build.js",
    "init": "npm install && cp node_modules/sortablejs/Sortable.min.js src/sortable.js"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "@types/node": "^20.12.7",
    "@types/sortablejs": "^1.15.8",
    "sortablejs": "^1.15.6",
    "typescript": "^5.4.5",
    "vite": "^7.0.4",
    "vite-plugin-static-copy": "^3.1.0"
  },
  "dependencies": {
  }
}
